# Leaderboard Backend - Environment Configuration
# Copy this file to .env and customize for your environment

# ========================================
# Database Configuration (Neon DB)
# ========================================
# Get your connection string from https://console.neon.tech
# Format: postgresql://user:password@host.neon.tech:5432/database?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_MHN2f1uwTngG@ep-cold-butterfly-ah9dxv1p-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Alternative: Individual connection parameters
DB_HOST=ep-your-project.neon.tech
DB_PORT=5432
DB_USER=neon_user
DB_PASSWORD=your_neon_password
DB_NAME=neon
DB_SSLMODE=require

# ========================================
# Cache Configuration (Redis)
# ========================================
REDIS_HOST=redis-16850.c265.us-east-1-2.ec2.cloud.redislabs.com
REDIS_PORT=16850
REDIS_PASSWORD=wYgS4jCT17I9Ww3JdCrKRn6clLIAB9us
REDIS_DB=0

# ========================================
# Server Configuration
# ========================================
PORT=8080
ENV=development

# ========================================
# Environment: development or production
# ========================================
# development: verbose logging, hot reload enabled
# production: minimal logging, optimized settings

# ========================================
# Usage in Docker
# ========================================
# docker run -e DATABASE_URL=postgresql://... \
#            -e REDIS_HOST=redis \
#            leaderboard-backend

# ========================================
# Notes
# ========================================
# - Neon DB connection string required (copy from console.neon.tech)
# - Redis must be running or use cloud Redis
# - All fields are required
# - DB_SSLMODE should be 'require' for Neon (not 'disable')
# - Rating range is hardcoded: 100-5000 (modify in service/validation.go)
# - Cache TTL values are hardcoded (modify in cache/cache.go)
# - Neon serverless: Tables auto-created on first run
# - Neon compute resources: Resume on connection (no data loss)
